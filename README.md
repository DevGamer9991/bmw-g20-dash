# Install Virtual Environment

```bash
python3 -m venv venv
```

```bash
source venv/bin/activate
```

```bash
pip install -r requirements.txt
```

# autostart filename
nano ~/.config/wayfire.ini

# remove the mouse cursor after 0 seconds of inactivity
unclutter -idle 0
